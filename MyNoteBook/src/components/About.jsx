import React  from 'react';
//import NoteContext from '../reactContext/notes/noteContext'  && { useContext } todo;

export default function About() {
   // const a = useContext(NoteContext);


    return (
        <div>
            About !!  
        </div>
    )
}
